# Allowlisted SHA-256 digests for release assets. The installer never trusts
# Update this file in the same reviewed change as herdr-plugin.toml when
# publishing a new hardened version. No legacy v0.2.2 assets are allowlisted:
# they predate the security fixes and must never be selected by a downgrade or
# a modified local manifest. Until v0.2.3 release assets are built and reviewed,
# installers intentionally compile the current source instead.
