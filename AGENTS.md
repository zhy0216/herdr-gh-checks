# Repository target

- The only authorized PR and release destination for this project is `zhy0216/herdr-gh-checks`.
- Always pass `--repo zhy0216/herdr-gh-checks` to `gh` commands that create or modify this project’s PRs or releases.
- Push this project’s branches and tags only to `origin`, which must point to `zhy0216/herdr-gh-checks`.
- Keep installer URLs and installation documentation pointed at `zhy0216/herdr-gh-checks`.
- Preserve the original copyright notice in `LICENSE`.
